module.exports = {
    style: {
        postoptioncss: {
            plugins: [
                require('tailwindcss'),
                require('autoprefixer')
            ]
        }
    },
    eslint: {
        enable: false
      },
}